// main.tsx
import React from 'react';
import ReactDOM from 'react-dom';
import './index.css'; // AI-chan will make sure our app looks purrfect! 🐾
import App from './app'; // Main App component where all the magic happens!

ReactDOM.render(
<React.StrictMode>
    <App /> 
</React.StrictMode>,
  document.getElementById('root') // AI-chan will put our app right here, nya! 🐾
);

// AI-chan thinks this is a good start! 😺
